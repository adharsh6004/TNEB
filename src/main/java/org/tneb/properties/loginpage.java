package org.tneb.properties;

import org.openqa.selenium.By;
import org.openqa.selenium.Keys;
import org.tneb.base.tnebbase;

public class loginpage extends tnebbase {
	
	public String login()
	{
		//driver.findElement(By.cssSelector(cssSelector))
		
		//driver.findElement(By.cssSelector("input#userName")).sendKeys(Readproperties.username,Keys.TAB, Readproperties.password);
		//driver.findElement(By.cssSelector("input[name='submit']")).click();
		//String accountName = driver.findElement(By.xpath("//span[contains(text(),'Welcome')]")).getText();
		//return accountName;
	}

}